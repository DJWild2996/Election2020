describe('Election 2017', function() {
	
	describe('The chart', function() {
		if('should exist', function() {
			expect(document.getElementById(myChart).toBeTruthy()
		})
	it('should have a Bar type', function() {
		expect(documnet.getElementsByName('bar')).toBeTruthy()
		})
	})
})
